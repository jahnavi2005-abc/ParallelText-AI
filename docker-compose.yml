version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PROJECT_NAME=Parallel Text Processor
      - DATABASE_URL=sqlite+aiosqlite:///./data/app.db
    volumes:
      - ./data:/app/data
